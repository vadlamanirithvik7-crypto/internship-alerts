name: Poll for new internships

on:
  schedule:
    # Every 5 minutes. GitHub may delay scheduled runs under load, which is fine -
    # receipts dedupe completed sends; the outbox retries unsuccessful deliveries.
    - cron: "2-59/5 * * * *"
  workflow_dispatch:
    inputs:
      discovery:
        description: "Run SEC sector discovery (slow, expands the watchlist)"
        type: boolean
        default: false
      board_slice:
        description: "How many company boards to poll this run"
        default: "250"

concurrency:
  # Never let a slow run overlap the next scheduled one and double-poll.
  group: poller
  cancel-in-progress: false

jobs:
  poll:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt pytest httpx numpy

      - name: Run tests
        env:
          AI_ENABLED: "0"
        run: python -m pytest -q

      - name: Poll sources and send alerts
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GOOGLE_SHEETS_WEBHOOK_URL: ${{ secrets.GOOGLE_SHEETS_WEBHOOK_URL }}
          GOOGLE_SHEETS_SYNC_TOKEN: ${{ secrets.GOOGLE_SHEETS_SYNC_TOKEN }}
          ALERT_EMAIL_FROM: ${{ secrets.ALERT_EMAIL_FROM }}
          ALERT_EMAIL_TO: ${{ secrets.ALERT_EMAIL_TO }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          NTFY_TOPIC: ${{ secrets.NTFY_TOPIC }}
          NTFY_TOKEN: ${{ secrets.NTFY_TOKEN }}
          NTFY_BASE: ${{ secrets.NTFY_BASE }}
          ADZUNA_APP_ID: ${{ secrets.ADZUNA_APP_ID }}
          ADZUNA_APP_KEY: ${{ secrets.ADZUNA_APP_KEY }}
          USAJOBS_API_KEY: ${{ secrets.USAJOBS_API_KEY }}
          USAJOBS_EMAIL: ${{ secrets.USAJOBS_EMAIL }}
          SEC_CONTACT_EMAIL: ${{ secrets.SEC_CONTACT_EMAIL }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REDDIT_CLIENT_ID: ${{ secrets.REDDIT_CLIENT_ID }}
          REDDIT_CLIENT_SECRET: ${{ secrets.REDDIT_CLIENT_SECRET }}
        run: |
          python3 poller/main.py \
            --board-slice "${{ github.event.inputs.board_slice || '250' }}" \
            --resolve-slice 0 \
            ${{ github.event.inputs.discovery == 'true' && '--discovery' || '' }} \
            --verbose

  # Weekly: expand the watchlist with new SEC-listed companies in our sectors,
  # then spend a bigger budget resolving them to live job boards.
  discover:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    if: github.event_name == 'schedule'
    # Only the Sunday 03:00 UTC tick should do discovery work.
    steps:
      - name: Check if weekly slot
        id: slot
        run: |
          if [ "$(date -u +%u)" = "7" ] && [ "$(date -u +%H)" = "03" ] && [ "$(date -u +%M)" -lt 5 ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/checkout@v4
        if: steps.slot.outputs.run == 'true'

      - uses: actions/setup-python@v5
        if: steps.slot.outputs.run == 'true'
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        if: steps.slot.outputs.run == 'true'
        run: pip install -r requirements.txt pytest httpx numpy

      - name: Discover and resolve companies
        if: steps.slot.outputs.run == 'true'
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          SEC_CONTACT_EMAIL: ${{ secrets.SEC_CONTACT_EMAIL }}
        run: |
          python3 poller/main.py \
            --discovery \
            --board-slice 0 \
            --resolve-slice 400 \
            --skip-alerts \
            --skip-search \
            --verbose
